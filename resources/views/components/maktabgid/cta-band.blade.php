<section class="section" style="padding-top:10px">
    <div class="wrap">
        <div class="cta-band">
            <div>
                <h2>{{ __('home.cta_title') }}</h2>
                <p>{{ __('home.cta_sub') }}</p>
            </div>
            <div class="cta-actions">
                <a class="btn btn-white" href="#"><x-maktabgid.icon name="plus" :width="18" :height="18" /> {{ __('home.cta_add_institution') }}</a>
                <a class="btn btn-outline-w" href="#">{{ __('home.details') }}</a>
            </div>
        </div>
    </div>
</section>
